[project]
name = "grounded-rag"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = [
  "ollama",
  "pdfplumber",
  "chromadb"
]

[tool.pytest.ini_options]
pythonpath = ["."]

[project.optional-dependencies]
dev = [
    "pip-tools",
    "pytest"
]
